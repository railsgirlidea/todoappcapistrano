<%= form_for(@todoo) do |f| %>
  <% if @todoo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todoo.errors.count, "error") %> prohibited this todoo from being saved:</h2>

      <ul>
      <% @todoo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :finished %><br>
    <%= f.check_box :finished %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
